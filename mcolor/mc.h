#ifndef MC_H
#define MC_H

#include <vector>

bool canColor(int m, int v, const std::vector<std::vector<int>>& graph, std::vector<int>& colors);
bool mColoring(const std::vector<std::vector<int>>& graph, int m);

#endif
